version: 2

build:
    os: ubuntu-20.04

conda:
    environment: .rtd-environment.yml

python:
    version: 3.8
    install:
      - method: pip
        path: .
        extra_requirements:
          - docs
